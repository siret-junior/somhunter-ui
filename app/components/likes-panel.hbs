<div class="likes-panel container fluid panel">
    <div class="row">
        <div class="col-12">

            <h2 class="panel-title small"> Likes</h2>
            <ul class="likes-list"
                {{did-update this.didUpdateAttrs @frames}}
            >
                
                {{#if this.items}}

                    {{#each this.items as |item|}}
                        {{#if item.src }}
                            <Frame @frame={{item}} />    
                        {{/if}}
                    {{/each}}                    

                {{/if}}

            </ul>
        </div>
    </div>
</div>